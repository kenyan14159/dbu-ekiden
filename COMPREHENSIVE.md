# 📚 大東文化大学 男子長距離ブロック 公式ウェブサイト 完全ガイド

このドキュメントは、プロジェクトの全情報を一箇所にまとめた包括的なリファレンスです。

---

# 目次

1. [プロジェクト概要 (README)](#1-プロジェクト概要)
2. [デザインシステム (DESIGN)](#2-デザインシステム)
3. [アーキテクチャ (ARCHITECTURE)](#3-アーキテクチャ)
4. [コンテンツ管理 (CONTENT)](#4-コンテンツ管理)
5. [データ構造 (DATA)](#5-データ構造)
6. [SEO設定 (SEO)](#6-seo設定)
7. [デプロイ (DEPLOYMENT)](#7-デプロイ)
8. [貢献ガイド (CONTRIBUTING)](#8-貢献ガイド)

---

# 1. プロジェクト概要

## 🌐 サイト情報

| 項目 | 内容 |
|------|------|
| **サイト名** | 大東文化大学陸上競技部男子長距離ブロック 公式ウェブサイト |
| **本番URL** | https://dbu-ekiden.com |
| **リポジトリ** | https://github.com/xxx/daito-ekiden |
| **ホスティング** | Cloudflare Pages / Vercel |
| **自動デプロイ** | GitHub Actions（mainブランチへのpush時） |

## 🏆 チームスローガン

> **歴史への礎 ～あの場所でやり返す〜**

## 🚀 技術スタック

### コア技術

| 技術 | バージョン | 用途 |
|------|-----------|------|
| Next.js | 16.x | フレームワーク（Turbopack） |
| React | 19.x | UIライブラリ |
| TypeScript | 5.x | 型安全な開発 |

### スタイリング・アニメーション

| 技術 | 用途 |
|------|------|
| Tailwind CSS 4 | ユーティリティファーストCSS |
| Framer Motion | 宣言的アニメーション |

### UIコンポーネント

| 技術 | 用途 |
|------|------|
| shadcn/ui | プリビルドコンポーネント |
| Radix UI | アクセシブルなUIプリミティブ |
| Lucide Icons | SVGアイコン |

### 分析・SEO（予定）

| 技術 | 用途 |
|------|------|
| Vercel Analytics | アクセス分析 |
| Vercel Speed Insights | パフォーマンス監視 |

## 📦 開発環境セットアップ

### 必要環境
- Node.js 20.x 以上
- npm または yarn

### クイックスタート

```bash
# 1. リポジトリクローン
git clone https://github.com/xxx/daito-ekiden.git
cd daito-ekiden

# 2. 依存関係インストール
npm install

# 3. 環境変数設定
cp .env.example .env.local
# .env.local を編集

# 4. 開発サーバー起動
npm run dev
# → http://localhost:3000
```

### 開発コマンド

```bash
npm run dev       # 開発サーバー起動
npm run build     # 静的サイト生成（SSG）
npm run start     # ビルド結果プレビュー
npm run typecheck # TypeScript型チェック（要追加）
npm run lint      # ESLintチェック
```

---

# 2. デザインシステム

## 🎨 カラーパレット

### CSS変数（HSL形式）

CSS変数ベースでライト/ダークモード両対応。

```css
:root {
  /* ========== ブランドカラー ========== */
  --daito-green: 147 100% 15%;        /* #004d25 - メインカラー */
  --daito-green-light: 147 60% 30%;   /* 明るめグリーン */
  --daito-green-dark: 147 100% 10%;   /* 暗めグリーン */
  --daito-orange: 36 100% 48%;        /* #f39800 - アクセントカラー */
  --daito-orange-light: 36 100% 60%;  /* 明るめオレンジ */
  --daito-gold: 43 74% 66%;           /* ゴールド */

  /* ========== 基本カラー ========== */
  --background: 0 0% 100%;            /* 純白 */
  --foreground: 0 0% 3.9%;            /* ほぼ黒 */
  
  /* プライマリ（大東グリーン） */
  --primary: 147 100% 15%;
  --primary-foreground: 0 0% 98%;
  
  /* アクセント（大東オレンジ） */
  --accent: 36 100% 48%;
  --accent-foreground: 0 0% 9%;
}
```

### ブランドカラー使用ガイド

| カラー | 用途 |
|--------|------|
| `daito-green` | メインカラー、ヘッダー、ボタン、重要な要素 |
| `daito-green-light` | ホバー状態、サブ要素 |
| `daito-green-dark` | 強調、フッター |
| `daito-orange` | アクセント、CTA、ハイライト |
| `daito-orange-light` | バッジ、装飾 |
| `daito-gold` | 特別な強調、受賞表示 |

## 🔤 タイポグラフィ

### フォント設定

```css
/* ヒーローセクション（和風表現） */
font-family: 'Noto Serif JP', serif;

/* 通常テキスト */
font-family: 'Noto Sans JP', ui-sans-serif, system-ui, sans-serif;
```

### レスポンシブテキストサイズ

| デバイス | ヒーローH1 | サブテキスト |
|----------|-----------|-------------|
| モバイル | `text-4xl` (2.25rem) | `text-sm` (0.875rem) |
| タブレット | `text-5xl-6xl` (3-3.75rem) | `text-base-lg` (1-1.125rem) |
| デスクトップ | `text-7xl` (4.5rem) | `text-lg-xl` (1.125-1.25rem) |

## 🧩 UIコンポーネント

### shadcn/ui コンポーネント

`app/components/ui/` に配置。Class Variance Authority (CVA) でバリアント管理。

#### 利用可能なコンポーネント

| カテゴリ | コンポーネント |
|---------|---------------|
| **フォーム** | Button, Input, Textarea |
| **表示** | Card, Badge, Skeleton, Separator |
| **ナビゲーション** | Tabs |
| **オーバーレイ** | Dialog, Tooltip, Accordion |

#### Button バリアント

```tsx
// バリアント
variant: 'default' | 'destructive' | 'outline' | 'secondary' | 'ghost' | 'link' | 'accent'

// サイズ
size: 'default' | 'sm' | 'lg' | 'icon'

// 使用例
<Button variant="default" size="lg">送信</Button>
<Button variant="accent">アクセントボタン</Button>
```

### カスタムコンポーネント

| コンポーネント | 場所 | 説明 |
|---------------|------|------|
| `Hero.tsx` | home/ | Framer Motionアニメーション付きヒーローセクション |
| `Header.tsx` | layout/ | レスポンシブナビゲーション（スクロール検知付き） |
| `Footer.tsx` | layout/ | サイトフッター |
| `LatestTopics.tsx` | home/ | 最新ニュース・リザルト表示 |
| `UpcomingEvents.tsx` | home/ | 今後の試合予定タイムライン |
| `SupportMessages.tsx` | home/ | 応援メッセージカルーセル |
| `TeamLinks.tsx` | home/ | チームコンテンツへのリンクグリッド |

## 🎬 アニメーション

### Framer Motionパターン

#### フェードイン + スライドアップ

```tsx
const itemVariants = {
  hidden: { opacity: 0, y: 30 },
  visible: {
    opacity: 1,
    y: 0,
    transition: {
      duration: 0.8,
      ease: [0.6, 0.05, 0.01, 0.9]
    }
  }
};

<motion.div
  initial="hidden"
  whileInView="visible"
  viewport={{ once: true }}
  variants={itemVariants}
>
  コンテンツ
</motion.div>
```

#### 文字ごとのスタッガーアニメーション

```tsx
const letterContainerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.03,
      delayChildren: 0.6
    }
  }
};

const letterVariants = {
  hidden: { opacity: 0, y: 10 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.4, ease: "easeOut" }
  }
};
```

### CSSアニメーション

#### Blobアニメーション（背景装飾）

```css
@keyframes blob {
  0%   { transform: translate(0px, 0px) scale(1); }
  33%  { transform: translate(30px, -50px) scale(1.1); }
  66%  { transform: translate(-20px, 20px) scale(0.9); }
  100% { transform: translate(0px, 0px) scale(1); }
}

.animate-blob { animation: blob 7s infinite; }
```

## 📐 レイアウト

### ブレークポイント

```
sm:  640px   - モバイル横向き
md:  768px   - タブレット
lg:  1024px  - デスクトップ
xl:  1280px  - 大画面
```

### コンテナパターン

```tsx
<div className="container mx-auto px-4">
  {/* 最大幅制限 + 中央配置 + パディング */}
</div>
```

## ♿ アクセシビリティ

### フォーカス表示

```css
*:focus-visible {
  outline: 2px solid hsl(var(--daito-green));
  outline-offset: 2px;
}
```

### モーション軽減設定

```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

### ARIA属性の使用例

```tsx
<button
  aria-label={isMenuOpen ? "メニューを閉じる" : "メニューを開く"}
  aria-expanded={isMenuOpen}
>

<div role="dialog" aria-modal="true" aria-label="ナビゲーションメニュー">
```

---

# 3. アーキテクチャ

## 📁 ディレクトリ構造

```
daito-ekiden/
├── app/                          # Next.js App Router
│   ├── layout.tsx               # ルートレイアウト
│   ├── page.tsx                 # トップページ
│   ├── globals.css              # グローバルスタイル
│   ├── sitemap.ts               # サイトマップ生成（予定）
│   │
│   ├── about/                   # チームについて
│   ├── contact/                 # お問い合わせ
│   ├── exclusive/               # 限定コンテンツ
│   ├── members/                 # メンバー紹介
│   ├── message/                 # 応援メッセージ
│   ├── news/                    # ニュース
│   ├── results/                 # リザルト
│   ├── schedule/                # スケジュール
│   ├── supporters/              # サポーター
│   │
│   └── components/              # Reactコンポーネント
│       ├── ui/                  # shadcn/ui コンポーネント
│       ├── home/                # ホーム用コンポーネント
│       ├── layout/              # レイアウト（Header, Footer）
│       ├── news/                # ニュース関連
│       ├── results/             # リザルト関連
│       ├── records/             # 記録関連
│       ├── team/                # チーム関連
│       └── events/              # イベント関連
│
├── lib/                          # ユーティリティ
│   ├── utils.ts                 # 汎用ユーティリティ
│   ├── structured-data.ts       # 構造化データ生成
│   └── seo-image-alt.ts         # SEO画像alt生成
│
├── public/                       # 静的ファイル
│   ├── data/                    # JSONデータ
│   │   ├── members/             # メンバーデータ
│   │   ├── records/             # 記録データ
│   │   ├── topics/              # ニュースデータ
│   │   └── events/              # イベントデータ
│   ├── images/                  # 画像
│   ├── robots.txt               # robots.txt
│   └── favicon.ico              # ファビコン
│
├── types/                        # TypeScript型定義
│   └── index.ts                 # 共通型定義
│
├── .github/
│   └── workflows/
│       └── deploy.yml           # デプロイワークフロー
│
├── next.config.ts               # Next.js設定
├── tailwind.config.ts           # Tailwind設定（Tailwind v4はCSS変数）
└── tsconfig.json                # TypeScript設定
```

## 🔄 データフロー

```
┌─────────────────────────────────────────────────────────────┐
│                      クライアント                           │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    Next.js App Router                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 ページコンポーネント                   │   │
│  │           (app/news/page.tsx 等)                    │   │
│  └──────────────────────────┬──────────────────────────┘   │
│                             │                               │
│  ┌──────────────────────────▼──────────────────────────┐   │
│  │                  lib/ ユーティリティ                  │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │   │
│  │  │  utils.ts   │  │structured-  │  │ seo-image-  │  │   │
│  │  │             │  │ data.ts     │  │  alt.ts     │  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  │   │
│  └──────────────────────────┬──────────────────────────┘   │
│                             │                               │
└─────────────────────────────┼───────────────────────────────┘
                              │
           ┌──────────────────┼──────────────────┐
           ▼                  ▼                  ▼
  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
  │ public/data/    │ │ content/*.mdx   │ │   外部API      │
  │  (JSON Files)   │ │  (MDX Files)    │ │  (将来対応)    │
  └─────────────────┘ └─────────────────┘ └─────────────────┘
```

## 📄 ページルーティング

Next.js App Routerの規約に従う。

| パス | ファイル | 説明 |
|------|---------|------|
| `/` | `app/page.tsx` | トップページ |
| `/members` | `app/members/page.tsx` | メンバー紹介 |
| `/news` | `app/news/page.tsx` | ニュース一覧 |
| `/results` | `app/results/page.tsx` | リザルト一覧 |
| `/schedule` | `app/schedule/page.tsx` | スケジュール |
| `/supporters` | `app/supporters/page.tsx` | サポーター紹介 |
| `/message` | `app/message/page.tsx` | 応援メッセージ |
| `/about` | `app/about/page.tsx` | ホームページについて |
| `/contact` | `app/contact/page.tsx` | お問い合わせ |
| `/exclusive` | `app/exclusive/page.tsx` | 限定コンテンツ |

---

# 4. コンテンツ管理

## 📰 ニュース記事の追加

### 方法1: JSONデータ（推奨）

`public/data/topics/news-articles-YYYY.json` を編集。

```json
{
  "articles": [
    {
      "id": 1,
      "slug": "2025-season-start",
      "title": "2025シーズン始動のお知らせ",
      "date": "2025-01-15",
      "category": "お知らせ",
      "excerpt": "2025年シーズンの練習が始まりました。",
      "imageUrl": "/images/news/2025-season-start.jpg"
    }
  ]
}
```

### 方法2: MDXファイル（将来対応）

`content/news/YYYY/` にMDXファイルを作成予定。

```mdx
---
title: "記事タイトル"
date: "2025-03-15"
description: "記事の説明"
category: "お知らせ"
---

記事本文をここに記述...
```

## 📊 リザルトの追加

`public/data/topics/results-YYYY.json` を編集。

```json
{
  "results": [
    {
      "id": 1,
      "slug": "hakone-2025",
      "title": "第101回箱根駅伝",
      "date": "2025-01-02",
      "competition": "箱根駅伝",
      "place": "10位",
      "summary": "往路12位、復路8位、総合10位"
    }
  ]
}
```

## 🖼️ 画像の追加

`public/images/` に画像を配置。

### ディレクトリ構成

```
public/images/
├── hero/               # ヒーロー画像
├── members/            # メンバー写真
├── news/               # ニュース画像
├── events/             # イベント画像
├── supporters/         # サポーター画像
└── ogp/                # OGP画像
```

### 推奨フォーマット

- 形式: JPEG/WebP
- 幅: 1920px以下
- ファイルサイズ: 500KB以下

---

# 5. データ構造

## 👥 メンバーデータ

`public/data/members/members-YYYY.json`

### スキーマ

```json
{
  "4年生": [
    {
      "name": "大東 太郎",
      "nameRubyParts": [
        { "kanji": "大東", "ruby": "だいとう" },
        { "kanji": "太郎", "ruby": "たろう" }
      ],
      "imageUrl": "/images/members/daito_tarou.jpeg",
      "highSchool": "○○高校",
      "faculty": "スポーツ・健康科学部",
      "department": "スポーツ科学科",
      "personalBests": [
        { "event": "5000m", "time": "13'50\"00" },
        { "event": "10000m", "time": "28'30\"00" },
        { "event": "ハーフマラソン", "time": "1:02'00" }
      ],
      "grade": 4,
      "profileUrl": "https://example.com/"
    }
  ],
  "3年生": [...],
  "2年生": [...],
  "1年生": [...]
}
```

### フィールド説明

| フィールド | 型 | 必須 | 説明 |
|-----------|------|-----|------|
| `name` | string | ○ | 選手名 |
| `nameRubyParts` | array | ○ | ルビ情報 |
| `imageUrl` | string | ○ | 画像パス |
| `highSchool` | string | ○ | 出身高校 |
| `faculty` | string | ○ | 学部 |
| `department` | string | ○ | 学科 |
| `personalBests` | array | ○ | 自己ベスト |
| `grade` | number | ○ | 学年 |
| `profileUrl` | string | - | プロフィールURL（オプション） |

## 🏃 記録データ

`public/data/records/` に種目別JSON。

### スキーマ

```json
{
  "5000m": [
    {
      "rank": 1,
      "name": "選手 太郎",
      "highSchool": "○○高校",
      "time": "13:32.58",
      "grade": "3年",
      "recordYear": "2024年",
      "competition": "日本選手権"
    }
  ]
}
```

### ファイル構成

```
public/data/records/
├── 5000m.json
├── 10000m.json
├── half-marathon.json
├── marathon.json
└── ekiden-legs.json       # 駅伝区間記録
```

## 📅 イベントデータ

`public/data/events/schedule-YYYY.json`

```json
{
  "events": [
    {
      "id": 1,
      "title": "第101回箱根駅伝",
      "date": "2025-01-02",
      "endDate": "2025-01-03",
      "location": "東京～箱根",
      "category": "駅伝",
      "description": "第101回東京箱根間往復大学駅伝競走"
    }
  ]
}
```

---

# 6. SEO設定

## 📊 構造化データ（JSON-LD）

`lib/structured-data.ts` でSchema.org準拠のJSON-LDを生成。

### 対応スキーマ

#### Organization（組織）

```tsx
import { generateOrganizationSchema } from '@/lib/structured-data';

const schema = generateOrganizationSchema();
// → 大東文化大学陸上競技部の組織情報
```

#### SportsTeam（スポーツチーム）

```tsx
import { generateSportsTeamSchema } from '@/lib/structured-data';

const schema = generateSportsTeamSchema({
  name: '大東文化大学陸上競技部男子長距離ブロック',
  sport: '陸上競技（駅伝・長距離）',
  url: 'https://dbu-ekiden.com/',
  memberOf: '大東文化大学'
});
```

#### BreadcrumbList（パンくずリスト）

```tsx
import { generateBreadcrumbSchema } from '@/lib/structured-data';

const schema = generateBreadcrumbSchema([
  { name: 'ホーム', url: 'https://dbu-ekiden.com/' },
  { name: 'ニュース', url: 'https://dbu-ekiden.com/news/' },
  { name: '記事タイトル', url: 'https://dbu-ekiden.com/news/slug/' }
]);
```

#### NewsArticle（ニュース記事）

```tsx
import { generateArticleSchema } from '@/lib/structured-data';

const schema = generateArticleSchema({
  headline: '記事タイトル',
  description: '記事の説明',
  datePublished: '2025-03-15',
  dateModified: '2025-03-16',
  image: '/images/news/xxx.jpg',
  url: 'https://dbu-ekiden.com/news/slug/'
});
```

#### Person（選手）

```tsx
import { generatePersonSchema } from '@/lib/structured-data';

const schema = generatePersonSchema({
  name: '大東 太郎',
  affiliation: '大東文化大学陸上競技部',
  alumniOf: '○○高校',
  url: 'https://dbu-ekiden.com/members/',
  image: '/images/members/xxx.jpg'
});
```

#### SportsEvent（スポーツイベント）

```tsx
import { generateSportsEventSchema } from '@/lib/structured-data';

const schema = generateSportsEventSchema({
  name: '第102回箱根駅伝',
  startDate: '2026-01-02',
  endDate: '2026-01-03',
  location: '東京〜箱根',
  url: 'https://dbu-ekiden.com/schedule/'
});
```

## 🖼️ 画像alt属性

`lib/seo-image-alt.ts` でSEO最適化されたalt属性を生成。

### ヘルパー関数

```tsx
import { 
  generatePlayerAlt,
  generateEventAlt,
  generateTrainingAlt,
  generateNewsAlt,
  generateGenericAlt 
} from '@/lib/seo-image-alt';

// 選手画像
generatePlayerAlt("大東太郎", "4年", "○○高校")
// → "大東太郎（4年・○○高校出身）- 大東文化大学駅伝部"

// 大会画像
generateEventAlt("箱根駅伝", "2025", "往路5区")
// → "2025年箱根駅伝 往路5区 - 大東文化大学駅伝部"

// 練習画像
generateTrainingAlt("夏合宿", "菅平高原", "2025")
// → "2025年夏合宿（菅平高原）- 大東文化大学駅伝部の練習風景"

// ニュース画像
generateNewsAlt("出雲駅伝エントリー決定", "2025-10-18")
// → "出雲駅伝エントリー決定（2025-10-18）- 大東文化大学駅伝部ニュース"
```

## 🗺️ サイトマップ

`app/sitemap.ts` で自動生成。

### 優先度設定

| ページ | priority | changeFrequency |
|--------|----------|-----------------|
| トップ | 1.0 | daily |
| メンバー | 0.8 | monthly |
| ニュース | 0.9 | daily |
| リザルト | 0.8 | weekly |
| スケジュール | 0.8 | weekly |
| その他 | 0.6 | monthly |

## 🤖 robots.txt

`public/robots.txt` に配置。

```
User-agent: *
Allow: /

Sitemap: https://dbu-ekiden.com/sitemap.xml
```

---

# 7. デプロイ

## 🚀 自動デプロイ（GitHub Actions）

`.github/workflows/deploy.yml` で設定。

### トリガー

- `main` ブランチへのpush
- 手動実行（workflow_dispatch）

### ワークフロー例（Cloudflare Pages）

```yaml
name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - run: npm ci
      - run: npm run build
      
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy out --project-name=daito-ekiden
```

### 必要なSecrets

| Secret | 説明 |
|--------|------|
| `CLOUDFLARE_API_TOKEN` | Cloudflare APIトークン |
| `CLOUDFLARE_ACCOUNT_ID` | CloudflareアカウントID |

## 🔧 手動デプロイ

```bash
# 1. ビルド
npm run build

# 2. デプロイ（Cloudflare）
npx wrangler pages deploy out --project-name=daito-ekiden

# または Vercel
vercel --prod
```

## 📋 デプロイチェックリスト

- [ ] TypeScriptエラーなし
- [ ] Lintエラーなし
- [ ] ビルド成功
- [ ] 環境変数設定済み
- [ ] 画像最適化済み
- [ ] sitemap.xml確認
- [ ] robots.txt確認
- [ ] OGP画像設定確認

---

# 8. 貢献ガイド

## 🌿 ブランチ戦略

```
main          # 本番環境（自動デプロイ）
├── feature/* # 新機能開発
├── fix/*     # バグ修正
└── docs/*    # ドキュメント更新
```

## 📝 コミットメッセージ

Conventional Commits準拠。

```
feat: 新機能追加
fix: バグ修正
docs: ドキュメント更新
style: コードスタイル変更
refactor: リファクタリング
perf: パフォーマンス改善
test: テスト追加
chore: ビルド・ツール変更
```

### 例

```bash
git commit -m "feat: 箱根駅伝の記録ページを追加"
git commit -m "fix: メンバー一覧の表示バグを修正"
git commit -m "docs: READMEのデプロイ手順を更新"
```

## 📋 プルリクエスト

1. フォーク（または新規ブランチ作成）
2. ブランチ作成 (`git checkout -b feature/amazing-feature`)
3. コミット
4. プッシュ (`git push origin feature/amazing-feature`)
5. PR作成

### PRテンプレート

```markdown
## 概要
変更内容の説明

## 変更種別
- [ ] 新機能
- [ ] バグ修正
- [ ] ドキュメント
- [ ] リファクタリング

## チェックリスト
- [ ] TypeScriptエラーなし
- [ ] Lintエラーなし
- [ ] ローカルでビルド確認済み
```

## 📏 コードスタイル

### TypeScript

- 厳格モード有効
- 明示的な型注釈推奨
- `any` 使用禁止

### コンポーネント

```tsx
// 1. "use client" ディレクティブ（必要な場合）
"use client";

// 2. インポート
import React from 'react';
import { Button } from '@/app/components/ui/button';

// 3. 型定義
interface Props {
  title: string;
  onClick?: () => void;
}

// 4. コンポーネント
export function MyComponent({ title, onClick }: Props) {
  return (
    <div>
      <h1>{title}</h1>
      <Button onClick={onClick}>クリック</Button>
    </div>
  );
}
```

### CSS

- Tailwind CSSユーティリティ優先
- カスタムCSSは `globals.css` の `@layer` で定義
- 命名規則: kebab-case

---

# 📞 サポート

- **ウェブサイト**: https://dbu-ekiden.com（予定）
- **GitHub Issues**: https://github.com/xxx/daito-ekiden/issues

---

# 参考リンク

- [Tailwind CSS](https://tailwindcss.com/docs)
- [shadcn/ui](https://ui.shadcn.com/)
- [Radix UI](https://www.radix-ui.com/)
- [Framer Motion](https://www.framer.com/motion/)
- [Lucide Icons](https://lucide.dev/icons/)
- [Next.js](https://nextjs.org/docs)

---

**© 2025 大東文化大学 陸上競技部男子長距離ブロック. All rights reserved.**
